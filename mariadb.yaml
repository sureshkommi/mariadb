- name : install mariadb
  hosts : db
  become : yes
  tasks :
    - name : install mariadb and mariadb server
      yum :
        name : "{{item}}"
        state : latest
      with_item :
        - mariadb
        - mariadb-server    
